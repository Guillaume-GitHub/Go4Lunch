<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="go4lunch" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/go4lunchPrimary</item>
        <item name="colorPrimaryDark">@color/go4lunchPrimaryDark</item>
        <item name="colorAccent">@color/go4lunchAccent</item>
    </style>

    <!-- rating bar theme. -->
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/unselectedItem</item>
        <item name="colorControlActivated">@color/go4lunchAccent</item>
    </style>
</resources>
